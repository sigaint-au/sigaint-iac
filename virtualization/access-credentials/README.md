# access-credentials

SSH public-key secrets for KubeVirt VMs (ExternalSecret / static keys).

| | |
|--|--|
| Cluster | `ocp` |
| App | `virt-access-credentials` |

```bash
kubectl kustomize virtualization/access-credentials/overlays/ocp
```
