# quay-operator

Quay Operator (OLM). Registry workload: `applications/quay`.

| | |
|--|--|
| Cluster | `ocp` |
| App | `infra-quay-operator` |
| Install NS | `openshift-operators` |

```bash
kubectl kustomize infrastructure/quay-operator/overlays/ocp
```
