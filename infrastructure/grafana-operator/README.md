# grafana-operator

Grafana Operator (OLM). Workload chart: `applications/grafana`.

| | |
|--|--|
| Cluster | `ocp` |
| App | `infra-grafana-operator` |

```bash
kubectl kustomize infrastructure/grafana-operator/overlays/ocp
```
