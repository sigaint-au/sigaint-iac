# openshift-console-operator

Console customization (branding / config).

| | |
|--|--|
| Clusters | `hub`, `ocp` |

```bash
kubectl kustomize infrastructure/openshift-console-operator/overlays/ocp
```
