# kiali-operator

Kiali Operator + mesh UI wiring (with Service Mesh).

| | |
|--|--|
| Cluster | `ocp` |
| App | `infra-kiali-operator` |

```bash
kubectl kustomize infrastructure/kiali-operator/overlays/ocp
```
