# cluster-observability-operator

Cluster Observability Operator (optional — not in ApplicationSets by default).

```bash
kubectl kustomize infrastructure/cluster-observability-operator/overlays/ocp
```
