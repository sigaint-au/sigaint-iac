apiVersion: v1
kind: Namespace
metadata:
  name: openshift-operators-redhat
  labels:
    # Scrape Loki Operator metrics via the platform Prometheus stack.
    openshift.io/cluster-monitoring: "true"
