apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  # Bootstrap secret is created out-of-band; never commit real tokens.
  # - doppler-secret.yaml.example
  - doppler-secretstore.yaml
