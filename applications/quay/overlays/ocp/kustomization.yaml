apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
components:
  - ../../../../components/operator-instance-sync-wave
namespace: sigaint-quay
resources:
  - ../../base
  - pipelines
  - external-secret-db.yaml
  - external-secret-config.yaml
  - quay-registry.yaml
  # Not used when internet connected.
  #- clair-update-cronjob.yaml